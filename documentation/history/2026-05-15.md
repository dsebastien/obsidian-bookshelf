# 2026-05-15

## Fixed Obsidian community plugin review warnings

- Bumped `manifest.json` `minAppVersion` from `1.4.0` → `1.10.0` to match the highest API used by the plugin. Resolves all "requires Obsidian v1.10.0" errors for the Bases API (`Plugin.registerBasesView`, `BasesView`, `BasesQueryResult`, `BasesViewConfig`, `BasesEntry`, `BasesEntryGroup`, `Value`) and the `Workspace.revealLeaf` (v1.7.2) error.
- Fixed the bare `/* eslint-disable @typescript-eslint/no-unused-vars */` directive in `src/app/types/bases.d.ts`: added a `-- reason` description and a matching `/* eslint-enable ... */` at the end of the file. The reviewer treats `eslint-enable` the same as `eslint-disable` for the "include descriptions" rule, so the closing directive also has a `-- reason` suffix.
- Did not edit `versions.json` — per project guidance the release workflow's version-bump script handles new entries on release.

## Verification

- `bun run tsc`: pass
- `bun run lint`: pass (0 warnings, `--max-warnings 0`)
- `bun test`: 31 pass / 0 fail
- `bun run build`: succeeded

## Manual verification needed

The catalog-side warnings on `Plugin.registerBasesView`, `Value`, `BasesView`, etc. should clear on the next release once a new tag is cut with the bumped `minAppVersion`.
